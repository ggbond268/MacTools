---
import BaseLayout from "@/layouts/BaseLayout.astro";
import SiteNav from "@/components/SiteNav.astro";
import SiteFooter from "@/components/SiteFooter.astro";
import {
  categoryLabel,
  hasPluginSettings,
  loadPluginCatalog,
  localizedPluginText,
} from "@/lib/plugins";

type LocalizedText = {
  zh: string;
  en: string;
};

type SettingOption = LocalizedText & {
  value: string;
};

type SettingRow = {
  title: LocalizedText;
  description: LocalizedText;
  control: "toggle" | "segmented" | "choice" | "select" | "slider" | "shortcut" | "button" | "status" | "text";
  enabled?: boolean;
  tone?: "success";
  value?: string | number;
  min?: number;
  max?: number;
  step?: number;
  unit?: string;
  options?: SettingOption[];
};

type SettingSection = {
  title: LocalizedText;
  icon: string;
  rows: SettingRow[];
};

const t = (zh: string, en: string): LocalizedText => ({ zh, en });
const option = (value: string, zh: string, en: string): SettingOption => ({ value, zh, en });
const symbol = (name: string) => `/assets/sf-symbols/${name.endsWith(".png") ? name : `${name}.png`}`;

const iconByPlugin: Record<string, string> = {
  "app-hotkey": "keyboard.png",
  "battery-charge-limit": "battery.100.bolt.png",
  "device-battery": "battery.100.png",
  "disk-clean": "internaldrive.png",
  "display-brightness": "sun.max.png",
  "fan-control": "fan.png",
  "fix-damaged-app": "hammer.png",
  homebrew: "shippingbox.fill.png",
  "ip-overview": "network.png",
  "keep-awake": "lamp.floor.png",
  "launch-control": "power.png",
  launchpad: "square.grid.3x3.fill.png",
  "middle-click": "cursorarrow.click.2.png",
  "mouse-enhancer": "computermouse.png",
  "right-click": "contextualmenu.and.cursorarrow.png",
  sidecar: "rectangle.2.swap.png",
  "system-status": "chart.bar.xaxis.png",
  translator: "text.bubble.png",
  "window-switcher": "macwindow.on.rectangle.png",
  "xcode-clean": "hammer.png",
  "zsh-config": "terminal.png",
};

const categoryIcons: Record<string, string> = {
  all: "square.grid.3x3.fill.png",
  display: "display.png",
  audio: "speaker.slash.png",
  system: "cpu.png",
  storage: "internaldrive.png",
  productivity: "sparkles.png",
  monitoring: "chart.bar.xaxis.png",
  other: "square.grid.2x2.png",
};

const categoryColors: Record<string, string> = {
  display: "#ffd60a",
  audio: "#ff453a",
  system: "#64d2ff",
  storage: "#30d158",
  productivity: "#bf5af2",
  monitoring: "#5ac8fa",
  other: "#8e8e93",
};

const localizedControlValues: Record<string, LocalizedText> = {
  "内置": t("内置", "Built-in"),
  "已启用": t("已启用", "Enabled"),
  "菜单栏右侧": t("菜单栏右侧", "Right side of menu bar"),
  "可用": t("可用", "Available"),
  "已授权": t("已授权", "Authorized"),
  "添加": t("添加", "Add"),
  "保存": t("保存", "Save"),
};

const settingsModels: Record<string, SettingSection[]> = {
  "window-switcher": [
    {
      title: t("快捷键", "Shortcut"),
      icon: "keyboard.png",
      rows: [
        {
          title: t("切换窗口", "Switch windows"),
          description: t("按下快捷键后显示当前正在运行的窗口。", "Show currently running windows when the shortcut is pressed."),
          control: "shortcut",
          value: "⌥ Tab",
        },
      ],
    },
    {
      title: t("外观", "Appearance"),
      icon: "macwindow.on.rectangle.png",
      rows: [
        {
          title: t("显示应用图标", "Show app icons"),
          description: t("在窗口缩略图旁显示对应的应用图标。", "Show each app icon beside its window thumbnail."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("窗口预览", "Window previews"),
          description: t("显示窗口内容缩略图，便于快速辨认。", "Display window thumbnails for faster recognition."),
          control: "toggle",
          enabled: true,
        },
      ],
    },
  ],
  "fan-control": [
    {
      title: t("内置预设", "Built-in presets"),
      icon: "fan.png",
      rows: [
        {
          title: t("自动", "Automatic"),
          description: t("由 macOS 自动管理", "Managed automatically by macOS"),
          control: "status",
          value: "内置",
        },
        {
          title: t("全速", "Full speed"),
          description: t("最高 6,800 RPM", "Up to 6,800 RPM"),
          control: "status",
          value: "内置",
        },
      ],
    },
    {
      title: t("自定义预设", "Custom presets"),
      icon: "slider.horizontal.3.png",
      rows: [
        {
          title: t("安静办公", "Quiet work"),
          description: t("固定风扇转速", "Fixed fan speed"),
          control: "slider",
          value: 2800,
          min: 1200,
          max: 6800,
          step: 100,
          unit: " RPM",
        },
        {
          title: t("新增预设", "Add preset"),
          description: t("创建一个可命名的固定转速方案。", "Create a named fixed-speed profile."),
          control: "button",
          value: "添加",
        },
      ],
    },
  ],
  "system-status": [
    {
      title: t("组件面板", "Component panel"),
      icon: "square.grid.2x2.png",
      rows: [
        {
          title: t("CPU", "CPU"),
          description: t("使用率、温度和功率", "Usage, temperature, and power"),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("内存", "Memory"),
          description: t("内存和交换空间", "Memory and swap"),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("网络", "Network"),
          description: t("上传、下载和地址", "Upload, download, and address"),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("磁盘", "Disk"),
          description: t("容量和读写速率", "Capacity and transfer rates"),
          control: "toggle",
          enabled: false,
        },
      ],
    },
    {
      title: t("菜单栏指标", "Menu bar metrics"),
      icon: "menubar.rectangle.png",
      rows: [
        {
          title: t("CPU 使用率", "CPU usage"),
          description: t("在菜单栏中显示实时百分比。", "Show the live percentage in the menu bar."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("上下行速率", "Network speed"),
          description: t("显示网络上传与下载速率。", "Show network upload and download speeds."),
          control: "toggle",
          enabled: false,
        },
      ],
    },
  ],
  launchpad: [
    {
      title: t("窗口", "Window"),
      icon: "macwindow.on.rectangle.png",
      rows: [
        {
          title: t("唤出方式", "Presentation"),
          description: t("屏幕中央的浮窗，点击窗外关闭。", "A centered window that closes when clicking outside."),
          control: "segmented",
          value: "compact",
          options: [option("compact", "紧凑", "Compact"), option("fullscreen", "全屏", "Full screen")],
        },
        {
          title: t("热区唤起", "Hot corner"),
          description: t("光标停在所选屏幕角落即唤出启动台。", "Open Launchpad by resting the pointer in a screen corner."),
          control: "select",
          value: "off",
          options: [
            option("off", "关闭", "Off"),
            option("top-left", "左上角", "Top left"),
            option("top-right", "右上角", "Top right"),
            option("bottom-left", "左下角", "Bottom left"),
            option("bottom-right", "右下角", "Bottom right"),
          ],
        },
      ],
    },
    {
      title: t("外观", "Appearance"),
      icon: "sparkles.png",
      rows: [
        {
          title: t("显示应用名称", "Show app names"),
          description: t("关闭后仅显示图标，排列更紧凑。", "Hide names for a tighter icon layout."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("图标大小", "Icon size"),
          description: t("每页行列数随之变化。", "Rows and columns adapt automatically."),
          control: "slider",
          value: 72,
          min: 48,
          max: 112,
          step: 8,
          unit: " pt",
        },
        {
          title: t("标签字号", "Label size"),
          description: t("与图标大小协调缩放。", "Scale labels to match the icons."),
          control: "segmented",
          value: "medium",
          options: [
            option("small", "小", "Small"),
            option("medium", "中", "Medium"),
            option("large", "大", "Large"),
          ],
        },
      ],
    },
  ],
  "device-battery": [
    {
      title: t("组件布局", "Widget layout"),
      icon: "square.grid.2x2.png",
      rows: [
        {
          title: t("排列方式", "Arrangement"),
          description: t("选择电池信息在组件中的排列方式。", "Choose how battery information is arranged."),
          control: "segmented",
          value: "adaptive",
          options: [
            option("list", "列表", "List"),
            option("grid", "网格", "Grid"),
            option("adaptive", "自适应", "Adaptive"),
          ],
        },
      ],
    },
    {
      title: t("显示内容", "Sources"),
      icon: "bolt.fill.png",
      rows: [
        {
          title: t("Mac 内置电池", "Mac battery"),
          description: t("显示本机电量、充电状态和剩余时间。", "Show charge, state, and remaining time."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("iPhone、iPad 与 Apple Watch", "iPhone, iPad, and Apple Watch"),
          description: t("读取已信任并连接的 Apple 移动设备。", "Read trusted and connected Apple mobile devices."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("蓝牙与 Apple 外设", "Bluetooth and Apple accessories"),
          description: t("显示 AirPods、Magic 外设及其他蓝牙设备。", "Show AirPods, Magic accessories, and Bluetooth devices."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("低电量通知", "Low-battery alerts"),
          description: t("设备电量低于 20% 时发送系统通知。", "Send a notification when a device drops below 20%."),
          control: "toggle",
          enabled: false,
        },
      ],
    },
  ],
  "keep-awake": [
    {
      title: t("屏幕", "Display"),
      icon: "display.png",
      rows: [
        {
          title: t("保持屏幕常亮", "Keep display on"),
          description: t("阻止休眠运行时，防止屏幕因空闲而关闭。", "Prevent the display from turning off while keeping the Mac awake."),
          control: "toggle",
          enabled: true,
        },
      ],
    },
    {
      title: t("MacBook", "MacBook"),
      icon: "laptopcomputer.png",
      rows: [
        {
          title: t("合盖保持唤醒", "Stay awake with lid closed"),
          description: t("仅在接通电源时生效。请保持 Mac 通风。", "Works only on external power. Keep your Mac ventilated."),
          control: "toggle",
          enabled: false,
        },
      ],
    },
  ],
  "display-brightness": [
    {
      title: t("作用范围", "Scope"),
      icon: "display.png",
      rows: [
        {
          title: t("快捷键目标", "Shortcut target"),
          description: t("调节鼠标所在的显示器，适合多显示器环境。", "Adjust the display under the pointer in multi-display setups."),
          control: "segmented",
          value: "pointer",
          options: [option("pointer", "鼠标所在", "Under pointer"), option("all", "所有显示器", "All displays")],
        },
      ],
    },
  ],
  "battery-charge-limit": [
    {
      title: t("充电上限", "Charge limit"),
      icon: "battery.100.bolt.png",
      rows: [
        {
          title: t("目标电量", "Target charge"),
          description: t("达到此电量后自动停止充电。", "Charging stops automatically at this level."),
          control: "slider",
          value: 80,
          min: 50,
          max: 100,
          step: 5,
          unit: "%",
        },
      ],
    },
    {
      title: t("充电行为", "Charging behavior"),
      icon: "bolt.fill.png",
      rows: [
        {
          title: t("不自动恢复充电", "Do not resume automatically"),
          description: t("电量低于上限时不会自动充电，需要手动点击「开始充电」。", "Charging remains paused below the limit until you resume it manually."),
          control: "status",
          value: "已启用",
        },
        {
          title: t("充电控制方式", "Control method"),
          description: t("当前 Mac 支持的 SMC 控制路径。", "The SMC control path supported by this Mac."),
          control: "status",
          value: "CH0B + CH0C",
        },
      ],
    },
  ],
  "mouse-enhancer": [
    {
      title: t("权限", "Permissions"),
      icon: "lock.png",
      rows: [
        {
          title: t("辅助功能", "Accessibility"),
          description: t("用于监听和调整滚动事件。", "Used to observe and adjust scrolling events."),
          control: "status",
          tone: "success",
          value: "已授权",
        },
        {
          title: t("输入监控", "Input Monitoring"),
          description: t("用于区分鼠标滚轮和触控板手势。", "Used to distinguish mouse-wheel events from trackpad gestures."),
          control: "status",
          tone: "success",
          value: "已授权",
        },
      ],
    },
    {
      title: t("鼠标", "Mouse"),
      icon: "computermouse.png",
      rows: [
        {
          title: t("垂直反转", "Reverse vertically"),
          description: t("反转鼠标上下滚动方向。", "Reverse the mouse's vertical scrolling direction."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("水平反转", "Reverse horizontally"),
          description: t("反转鼠标左右滚动方向。", "Reverse the mouse's horizontal scrolling direction."),
          control: "toggle",
          enabled: false,
        },
      ],
    },
    {
      title: t("触控板", "Trackpad"),
      icon: "computermouse.fill.png",
      rows: [
        {
          title: t("垂直反转", "Reverse vertically"),
          description: t("反转触控板和 Magic Mouse 上下滚动方向。", "Reverse vertical scrolling on the trackpad and Magic Mouse."),
          control: "toggle",
          enabled: false,
        },
        {
          title: t("水平反转", "Reverse horizontally"),
          description: t("反转触控板和 Magic Mouse 左右滚动方向。", "Reverse horizontal scrolling on the trackpad and Magic Mouse."),
          control: "toggle",
          enabled: false,
        },
      ],
    },
  ],
  "middle-click": [
    {
      title: t("权限", "Permissions"),
      icon: "lock.png",
      rows: [
        {
          title: t("辅助功能", "Accessibility"),
          description: t("用于将触控板轻点转换为鼠标中键点击。", "Used to turn trackpad taps into middle-button clicks."),
          control: "status",
          tone: "success",
          value: "已授权",
        },
      ],
    },
    {
      title: t("设置", "Settings"),
      icon: "cursorarrow.click.2.png",
      rows: [
        {
          title: t("模拟鼠标中键", "Middle Click"),
          description: t("启用后，用指定数量的手指轻点触控板来模拟鼠标中键。", "When enabled, tap the trackpad with the selected number of fingers to simulate a middle click."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("手指数量", "Finger count"),
          description: t("用指定数量的手指在触控板上轻点，将模拟鼠标中键点击。", "Tap with the selected number of fingers to simulate a middle click."),
          control: "choice",
          value: "3",
          options: [
            option("3", "3 指", "3 fingers"),
            option("4", "4 指", "4 fingers"),
            option("5", "5 指", "5 fingers"),
          ],
        },
      ],
    },
  ],
  translator: [
    {
      title: t("翻译服务", "Translation providers"),
      icon: "text.bubble.png",
      rows: [
        {
          title: t("服务名称", "Provider name"),
          description: t("用于在服务列表中识别此配置。", "A name used to identify this configuration."),
          control: "text",
          value: "OpenAI",
        },
        {
          title: t("接口地址", "Base URL"),
          description: t("兼容 OpenAI API 的服务地址。", "The endpoint for an OpenAI-compatible API."),
          control: "text",
          value: "https://api.openai.com",
        },
        {
          title: t("模型", "Model"),
          description: t("用于翻译的模型名称。", "The model used for translation."),
          control: "text",
          value: "gpt-5.4-mini",
        },
        {
          title: t("保存配置", "Save configuration"),
          description: t("接口密钥会安全存储在系统钥匙串中。", "API keys are stored securely in Keychain."),
          control: "button",
          value: "保存",
        },
      ],
    },
  ],
  sidecar: [
    {
      title: t("连接", "Connection"),
      icon: "rectangle.2.swap.png",
      rows: [
        {
          title: t("默认设备", "Default device"),
          description: t("打开功能面板时优先连接的 Sidecar 设备。", "The Sidecar device preferred when opening the feature panel."),
          control: "select",
          value: "ipad",
          options: [option("ipad", "明秋的 iPad", "Mingqiu's iPad"), option("ask", "每次询问", "Ask every time")],
        },
        {
          title: t("自动连接", "Connect automatically"),
          description: t("设备可用时立即开始 Sidecar 连接。", "Start Sidecar as soon as the device is available."),
          control: "toggle",
          enabled: false,
        },
      ],
    },
  ],
  "disk-clean": [
    {
      title: t("扫描范围", "Scan scope"),
      icon: "internaldrive.png",
      rows: [
        {
          title: t("应用缓存", "Application caches"),
          description: t("扫描用户缓存目录中的可安全清理内容。", "Scan safe-to-remove content in user cache folders."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("浏览器缓存", "Browser caches"),
          description: t("包含 Safari、Chrome 与其他常见浏览器缓存。", "Include Safari, Chrome, and other common browser caches."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("开发者缓存", "Developer caches"),
          description: t("查找构建缓存与模拟器临时文件。", "Find build caches and simulator temporary files."),
          control: "toggle",
          enabled: false,
        },
      ],
    },
  ],
  "xcode-clean": [
    {
      title: t("清理分类", "Cleanup categories"),
      icon: "hammer.png",
      rows: [
        {
          title: t("DerivedData", "DerivedData"),
          description: t("移除可由 Xcode 重新生成的构建产物。", "Remove build products Xcode can regenerate."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("设备支持", "Device support"),
          description: t("清理旧版 iOS 设备符号与支持文件。", "Clean symbols and support files for older iOS versions."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("归档", "Archives"),
          description: t("归档可能包含仍需分发的构建，请谨慎选择。", "Archives may contain distributable builds; review before cleaning."),
          control: "toggle",
          enabled: false,
        },
      ],
    },
  ],
  "right-click": [
    {
      title: t("Finder 右键菜单", "Finder contextual menu"),
      icon: "contextualmenu.and.cursorarrow.png",
      rows: [
        {
          title: t("新建文件", "New file"),
          description: t("在当前 Finder 文件夹中创建空白文件。", "Create an empty file in the current Finder folder."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("复制路径", "Copy path"),
          description: t("复制所选项目的完整 POSIX 路径。", "Copy the selected item's full POSIX path."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("在终端中打开", "Open in Terminal"),
          description: t("从当前文件夹直接打开终端窗口。", "Open a Terminal window at the current folder."),
          control: "toggle",
          enabled: true,
        },
      ],
    },
  ],
  "fix-damaged-app": [
    {
      title: t("处理方式", "Repair"),
      icon: "wrench.and.screwdriver.fill.png",
      rows: [
        {
          title: t("默认位置", "Default location"),
          description: t("选择应用时默认打开的文件夹。", "The folder opened first when choosing an app."),
          control: "select",
          value: "applications",
          options: [option("applications", "应用程序", "Applications"), option("desktop", "桌面", "Desktop")],
        },
        {
          title: t("命令行工具", "Command-line tool"),
          description: t("使用系统 xattr 移除所选应用的隔离属性。", "Use the system xattr tool to remove quarantine attributes."),
          control: "status",
          value: "可用",
        },
      ],
    },
  ],
  "app-hotkey": [
    {
      title: t("应用快捷键", "App hotkeys"),
      icon: "keyboard.png",
      rows: [
        {
          title: t("Safari", "Safari"),
          description: t("打开应用，或切换到最前方。", "Open the app or bring it to the front."),
          control: "shortcut",
          value: "⌃ S",
        },
        {
          title: t("终端", "Terminal"),
          description: t("打开应用，或切换到最前方。", "Open the app or bring it to the front."),
          control: "shortcut",
          value: "⌃ T",
        },
        {
          title: t("添加应用", "Add app"),
          description: t("从“应用程序”文件夹选择另一个应用。", "Choose another app from the Applications folder."),
          control: "button",
          value: "添加",
        },
      ],
    },
  ],
  homebrew: [
    {
      title: t("更新", "Updates"),
      icon: "shippingbox.fill.png",
      rows: [
        {
          title: t("自动刷新软件包信息", "Refresh package information"),
          description: t("打开面板时检查 formula 和 cask 更新。", "Check formula and cask updates when the panel opens."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("更新间隔", "Update interval"),
          description: t("控制后台刷新频率。", "Control how often information is refreshed."),
          control: "select",
          value: "daily",
          options: [option("hourly", "每小时", "Hourly"), option("daily", "每天", "Daily"), option("manual", "手动", "Manual")],
        },
      ],
    },
  ],
  "ip-overview": [
    {
      title: t("网络检测", "Network checks"),
      icon: "network.png",
      rows: [
        {
          title: t("显示出口 IP", "Show public IP"),
          description: t("获取当前网络对外显示的 IPv4 或 IPv6 地址。", "Fetch the public IPv4 or IPv6 address for the active network."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("显示归属地", "Show location"),
          description: t("显示 IP 所属国家、地区和网络运营商。", "Show the country, region, and provider for the IP address."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("测速服务器", "Speed test server"),
          description: t("选择延迟和下载测试使用的节点。", "Choose the endpoint used for latency and download tests."),
          control: "select",
          value: "auto",
          options: [option("auto", "自动选择", "Automatic"), option("china", "中国大陆", "China mainland"), option("global", "全球", "Global")],
        },
      ],
    },
  ],
  "launch-control": [
    {
      title: t("启动项", "Launch items"),
      icon: "power.png",
      rows: [
        {
          title: t("显示用户启动项", "Show user agents"),
          description: t("包含当前用户 Library 中的 LaunchAgents。", "Include LaunchAgents in the current user's Library."),
          control: "toggle",
          enabled: true,
        },
        {
          title: t("显示系统启动项", "Show system items"),
          description: t("包含系统级 LaunchAgents 与 LaunchDaemons。", "Include system LaunchAgents and LaunchDaemons."),
          control: "toggle",
          enabled: true,
        },
      ],
    },
  ],
  "zsh-config": [
    {
      title: t("配置文件", "Configuration file"),
      icon: "terminal.png",
      rows: [
        {
          title: t("默认文件", "Default file"),
          description: t("打开编辑器时默认加载的 zsh 配置文件。", "The zsh configuration loaded when the editor opens."),
          control: "select",
          value: "zshrc",
          options: [option("zshrc", "~/.zshrc", "~/.zshrc"), option("zprofile", "~/.zprofile", "~/.zprofile"), option("zshenv", "~/.zshenv", "~/.zshenv")],
        },
        {
          title: t("保存前创建备份", "Back up before saving"),
          description: t("写入前在原文件旁保留一个时间戳副本。", "Keep a timestamped copy beside the original before writing."),
          control: "toggle",
          enabled: true,
        },
      ],
    },
  ],
};

void iconByPlugin;
void settingsModels;

const catalog = await loadPluginCatalog();
const plugins = catalog.plugins.filter((plugin) => plugin.id !== "menu-bar-hidden").sort((a, b) =>
  localizedPluginText(a).zh.displayName.localeCompare(localizedPluginText(b).zh.displayName, "zh-CN")
);
const settingsPlugins = plugins.filter(hasPluginSettings);

const categoryOrder = ["display", "audio", "system", "storage", "productivity", "monitoring", "other"];
const counts = new Map<string, number>();
for (const plugin of plugins) {
  const key = plugin.category ?? "other";
  counts.set(key, (counts.get(key) ?? 0) + 1);
}
const categoryFilters = categoryOrder
  .filter((key) => counts.has(key))
  .map((key) => ({
    key,
    count: counts.get(key) ?? 0,
    label: categoryLabel(key === "other" ? null : key),
  }));

const fallbackSections = (name: LocalizedText): SettingSection[] => [
  {
    title: t("基本设置", "General"),
    icon: "slider.horizontal.3.png",
    rows: [
      {
        title: t(`启用${name.zh}`, `Enable ${name.en}`),
        description: t("控制此插件在 MacTools 中的主要功能。", "Control this plugin's primary behavior in MacTools."),
        control: "toggle",
        enabled: true,
      },
    ],
  },
];

const pageTitle = "MacTools 插件与设置 | macOS 菜单栏工具";
const pageDescription = `在仿 macOS 设置窗口中浏览 ${plugins.length} 个 MacTools 插件，搜索分类，并交互预览窗口切换、风扇、系统状态、启动台、电池等插件设置。`;
const structuredData = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  name: pageTitle,
  description: pageDescription,
  url: new URL(Astro.url.pathname, Astro.site).href,
  mainEntity: {
    "@type": "ItemList",
    numberOfItems: plugins.length,
    itemListElement: plugins.map((plugin, index) => {
      const text = localizedPluginText(plugin);
      return {
        "@type": "ListItem",
        position: index + 1,
        name: text.en.displayName || text.zh.displayName,
        description: text.en.summary || text.zh.summary,
      };
    }),
  },
};
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
  image="/assets/screenshots/settings-plugins-zh-dark.png"
  structuredData={structuredData}
  page="plugins"
>
  <SiteNav active="plugins" />
  <main>
    <section class="page-hero plugins-hero">
      <p class="eyebrow">
        <span data-i18n-zh>MacTools 插件生态</span>
        <span data-i18n-en>MacTools Plugin Ecosystem</span>
      </p>
      <h1>
        <span data-i18n-zh>丰富插件，灵活设置。</span>
        <span data-i18n-en>More plugins. Your settings.</span>
      </h1>
      <p class="plugins-hero-lead">
        <span data-i18n-zh>{plugins.length} 个原生插件覆盖显示、音频、系统、清理、效率与监控。按需组合，每个插件都有清晰独立的设置。</span>
        <span data-i18n-en>{plugins.length} native plugins for displays, audio, system controls, cleanup, productivity, and monitoring. Mix what you need and fine-tune each one.</span>
      </p>
    </section>

    <section
      class="plugins-workspace"
      aria-label="MacTools 插件设置交互预览"
      data-aria-label-zh="MacTools 插件设置交互预览"
      data-aria-label-en="MacTools plugin settings interactive preview"
    >
      <div class="settings-window" data-settings-window>
        <div class="settings-titlebar" aria-hidden="true">
          <div class="traffic-lights">
            <span></span>
            <span></span>
            <span></span>
          </div>
        </div>

        <div class="settings-window-body">
          <aside
            class="settings-sidebar"
            aria-label="插件设置页面"
            data-aria-label-zh="插件设置页面"
            data-aria-label-en="Plugin settings pages"
          >
            <div class="settings-sidebar-scroll">
              <p class="settings-sidebar-label">
                <span data-i18n-zh>插件</span>
                <span data-i18n-en>Plugins</span>
              </p>
              <button class="settings-sidebar-item is-selected" type="button" data-settings-tab="market" aria-selected="true">
                <span
                  class="sidebar-icon sidebar-icon-market"
                  style={`--sidebar-icon: url("${symbol("shippingbox.fill")}"); --sidebar-icon-color: #0a84ff;`}
                ></span>
                <span>
                  <span data-i18n-zh>插件市场</span>
                  <span data-i18n-en>Plugin Marketplace</span>
                </span>
              </button>

              <p class="settings-sidebar-label settings-sidebar-plugin-label">
                <span data-i18n-zh>插件设置</span>
                <span data-i18n-en>Plugin Settings</span>
              </p>
              {settingsPlugins.map((plugin) => {
                const text = localizedPluginText(plugin);
                return (
                  <button
                    type="button"
                    class="settings-sidebar-item"
                    data-settings-tab={plugin.id}
                    data-plugin-name-zh={text.zh.displayName}
                    data-plugin-name-en={text.en.displayName}
                    data-plugin-id={plugin.id}
                    aria-selected="false"
                  >
                    <span
                      class="sidebar-icon"
                      style={`--sidebar-icon: url("${symbol(categoryIcons[plugin.category ?? "other"])}"); --sidebar-icon-color: ${categoryColors[plugin.category ?? "other"]};`}
                    ></span>
                    <span>
                      <span data-i18n-zh>{text.zh.displayName}</span>
                      <span data-i18n-en>{text.en.displayName}</span>
                    </span>
                  </button>
                );
              })}
            </div>
          </aside>

          <div class="settings-content">
            <section class="settings-panel marketplace-panel" data-settings-panel="market">
              <header class="settings-page-header">
                <h2>
                  <span data-i18n-zh>市场</span>
                  <span data-i18n-en>Marketplace</span>
                </h2>
              </header>

              <label class="market-search">
                <img src={symbol("magnifyingglass")} alt="" />
                <span class="sr-only">
                  <span data-i18n-zh>搜索插件</span>
                  <span data-i18n-en>Search plugins</span>
                </span>
                <input
                  type="search"
                  data-market-search
                  placeholder="搜索插件名称或简介"
                  data-placeholder-zh="搜索插件名称或简介"
                  data-placeholder-en="Search plugin name or summary"
                />
              </label>

              <div
                class="market-categories"
                aria-label="插件分类"
                data-aria-label-zh="插件分类"
                data-aria-label-en="Plugin categories"
              >
                <button class="is-selected" type="button" data-market-filter="all" aria-pressed="true">
                  <img src={symbol(categoryIcons.all)} alt="" />
                  <span data-i18n-zh>全部</span>
                  <span data-i18n-en>All</span>
                  <strong>{plugins.length}</strong>
                </button>
                {categoryFilters.map((filter) => (
                  <button type="button" data-market-filter={filter.key} aria-pressed="false" data-category={filter.key}>
                    <img src={symbol(categoryIcons[filter.key])} alt="" />
                    <span data-i18n-zh>{filter.label.zh}</span>
                    <span data-i18n-en>{filter.label.en}</span>
                    <strong>{filter.count}</strong>
                  </button>
                ))}
              </div>

              <p class="market-result-summary" aria-live="polite">
                <span data-i18n-zh>显示 <strong data-result-count>{plugins.length}</strong> 个插件</span>
                <span data-i18n-en><strong data-result-count>{plugins.length}</strong> plugins</span>
              </p>

              <div class="market-list">
                {plugins.map((plugin) => {
                  const text = localizedPluginText(plugin);
                  const category = plugin.category ?? "other";
                  const isInitiallyInstalled = hasPluginSettings(plugin);
                  const capability = hasPluginSettings(plugin)
                    ? t("功能面板 · 可配置", "Feature panel · Configurable")
                    : plugin.capabilities.componentPanel
                      ? t("仪表盘组件", "Dashboard widget")
                      : t("快捷操作", "Quick action");
                  const searchValue = [
                    text.zh.displayName,
                    text.zh.summary,
                    text.en.displayName,
                    text.en.summary,
                  ].join(" ").toLocaleLowerCase();
                  return (
                    <article
                      class="market-plugin-row"
                      data-market-plugin
                      data-category={category}
                      data-search={searchValue}
                      data-plugin-name-zh={text.zh.displayName}
                      data-plugin-name-en={text.en.displayName}
                      data-plugin-id={plugin.id}
                      data-installed={String(isInitiallyInstalled)}
                    >
                      <span
                        class="market-plugin-status"
                        aria-label={isInitiallyInstalled ? "已安装" : "未安装"}
                        data-aria-label-zh={isInitiallyInstalled ? "已安装" : "未安装"}
                        data-aria-label-en={isInitiallyInstalled ? "Installed" : "Not installed"}
                      ></span>
                      <div class="market-plugin-copy">
                        <h3>
                          <span data-i18n-zh>{text.zh.displayName}</span>
                          <span data-i18n-en>{text.en.displayName}</span>
                          <small>{plugin.version}</small>
                        </h3>
                        <p>
                          <span data-i18n-zh>{text.zh.summary} · {capability.zh}</span>
                          <span data-i18n-en>{text.en.summary} · {capability.en}</span>
                        </p>
                        <div class="market-plugin-links">
                          <a class="market-plugin-detail-link" href={plugin.route ?? `/plugins/${plugin.id}`}>
                            <span data-i18n-zh>查看详情</span>
                            <span data-i18n-en>View details</span>
                          </a>
                          <a class="market-plugin-open-link" href={plugin.openInMacToolsURL}>
                            <span data-i18n-zh>在 MacTools 中打开</span>
                            <span data-i18n-en>Open in MacTools</span>
                          </a>
                        </div>
                      </div>
                      <div class="market-plugin-actions">
                        <button type="button" class="market-plugin-action" data-plugin-action>
                          <span class="market-action-install">
                            <span data-i18n-zh>安装</span>
                            <span data-i18n-en>Install</span>
                          </span>
                          <span class="market-action-uninstall">
                            <span data-i18n-zh>卸载</span>
                            <span data-i18n-en>Uninstall</span>
                          </span>
                        </button>
                      </div>
                    </article>
                  );
                })}
                <div class="market-empty" data-market-empty hidden>
                  <img src={symbol("magnifyingglass")} alt="" />
                  <h3>
                    <span data-i18n-zh>未找到匹配的插件</span>
                    <span data-i18n-en>No matching plugins</span>
                  </h3>
                  <p>
                    <span data-i18n-zh>尝试调整关键字或切换分类。</span>
                    <span data-i18n-en>Try another keyword or category.</span>
                  </p>
                </div>
              </div>
            </section>

            {settingsPlugins.map((plugin) => {
              const text = localizedPluginText(plugin);
              const sections = fallbackSections({
                zh: text.zh.displayName,
                en: text.en.displayName,
              });
              return (
                <section class="settings-panel plugin-settings-panel" data-settings-panel={plugin.id} hidden>
                  <header class="plugin-settings-header">
                    <span
                      class="plugin-settings-icon"
                      style={`--plugin-icon: url("${symbol(categoryIcons[plugin.category ?? "other"])}"); --plugin-icon-color: ${categoryColors[plugin.category ?? "other"]};`}
                    ></span>
                    <div>
                      <h2>
                        <span data-i18n-zh>{text.zh.displayName}</span>
                        <span data-i18n-en>{text.en.displayName}</span>
                        <small>{plugin.version}</small>
                      </h2>
                      <p>
                        <span data-i18n-zh>{text.zh.summary}</span>
                        <span data-i18n-en>{text.en.summary}</span>
                      </p>
                      <div class="plugin-settings-catalog-links">
                        <a href={plugin.route ?? `/plugins/${plugin.id}`}>
                          <span data-i18n-zh>查看插件详情</span>
                          <span data-i18n-en>View plugin details</span>
                        </a>
                        <a href={plugin.openInMacToolsURL}>
                          <span data-i18n-zh>在 MacTools 中打开</span>
                          <span data-i18n-en>Open in MacTools</span>
                        </a>
                      </div>
                    </div>
                  </header>

                  <div class="plugin-settings-sections">
                    {sections.map((section) => (
                      <section class="settings-form-section">
                        <h3>
                          <span
                            class="settings-section-icon"
                            style={`--section-icon: url("${symbol(section.icon)}");`}
                          ></span>
                          <span data-i18n-zh>{section.title.zh}</span>
                          <span data-i18n-en>{section.title.en}</span>
                        </h3>
                        <div class="settings-card">
                          {section.rows.map((row) => (
                            <div class={`settings-row settings-row--${row.control}`}>
                              <div class="settings-row-copy">
                                <h4>
                                  <span data-i18n-zh>{row.title.zh}</span>
                                  <span data-i18n-en>{row.title.en}</span>
                                </h4>
                                <p>
                                  <span data-i18n-zh>{row.description.zh}</span>
                                  <span data-i18n-en>{row.description.en}</span>
                                </p>
                              </div>
                              <div class="settings-control">
                                {row.control === "toggle" ? (
                                  <button
                                    class="mac-switch"
                                    type="button"
                                    role="switch"
                                    aria-checked={row.enabled ?? false}
                                    data-setting-switch
                                  >
                                    <span></span>
                                  </button>
                                ) : null}
                                {row.control === "segmented" ? (
                                  <div class="segmented-control" data-segmented-control>
                                    {row.options?.map((item) => (
                                      <button
                                        type="button"
                                        class={item.value === row.value ? "is-selected" : ""}
                                        aria-pressed={item.value === row.value}
                                      >
                                        <span data-i18n-zh>{item.zh}</span>
                                        <span data-i18n-en>{item.en}</span>
                                      </button>
                                    ))}
                                  </div>
                                ) : null}
                                {row.control === "choice" ? (
                                  <div class="choice-control" data-segmented-control>
                                    {row.options?.map((item) => (
                                      <button
                                        type="button"
                                        class={item.value === row.value ? "is-selected" : ""}
                                        aria-pressed={item.value === row.value}
                                      >
                                        <span class="choice-symbol" aria-hidden="true"></span>
                                        <span data-i18n-zh>{item.zh}</span>
                                        <span data-i18n-en>{item.en}</span>
                                      </button>
                                    ))}
                                  </div>
                                ) : null}
                                {row.control === "select" ? (
                                  <select
                                    aria-label={row.title.zh}
                                    data-aria-label-zh={row.title.zh}
                                    data-aria-label-en={row.title.en}
                                    value={String(row.value ?? "")}
                                  >
                                    {row.options?.map((item) => (
                                      <option
                                        value={item.value}
                                        selected={item.value === row.value}
                                        data-option-zh={item.zh}
                                        data-option-en={item.en}
                                      >
                                        {item.zh}
                                      </option>
                                    ))}
                                  </select>
                                ) : null}
                                {row.control === "slider" ? (
                                  <div class="range-control">
                                    <input
                                      type="range"
                                      min={row.min}
                                      max={row.max}
                                      step={row.step}
                                      value={row.value}
                                      data-range-control
                                      aria-label={row.title.zh}
                                      data-aria-label-zh={row.title.zh}
                                      data-aria-label-en={row.title.en}
                                    />
                                    <output>{row.value}{row.unit}</output>
                                  </div>
                                ) : null}
                                {row.control === "shortcut" ? <kbd class="shortcut-key">{row.value}</kbd> : null}
                                {row.control === "button" ? (
                                  <button type="button" class="settings-action-button" data-demo-action>
                                    <span data-i18n-zh>{localizedControlValues[String(row.value)]?.zh ?? row.value}</span>
                                    <span data-i18n-en>{localizedControlValues[String(row.value)]?.en ?? row.value}</span>
                                  </button>
                                ) : null}
                                {row.control === "status" ? (
                                  <span class:list={["settings-status", row.tone === "success" && "is-success"]}>
                                    <span data-i18n-zh>{localizedControlValues[String(row.value)]?.zh ?? row.value}</span>
                                    <span data-i18n-en>{localizedControlValues[String(row.value)]?.en ?? row.value}</span>
                                  </span>
                                ) : null}
                                {row.control === "text" ? (
                                  <input
                                    class="settings-text-input"
                                    type="text"
                                    value={String(row.value ?? "")}
                                    aria-label={row.title.zh}
                                    data-aria-label-zh={row.title.zh}
                                    data-aria-label-en={row.title.en}
                                  />
                                ) : null}
                              </div>
                            </div>
                          ))}
                        </div>
                      </section>
                    ))}
                  </div>
                </section>
              );
            })}
          </div>
        </div>
      </div>
      <p class="settings-demo-note">
        <span data-i18n-zh>此页面仅作界面与交互演示，实际功能及可用选项以 MacTools 软件为准。</span>
        <span data-i18n-en>This page is a UI and interaction preview. Actual features and options may differ in MacTools.</span>
      </p>
    </section>
  </main>
  <SiteFooter />
  <script src="../scripts/site-controls.ts"></script>
  <script src="../scripts/plugin-settings.ts"></script>
</BaseLayout>
