settings:
  base:
    OTHER_LDFLAGS: -framework IOKit

targets:
  mactools-fan-smc-helper:
    type: tool
    platform: macOS
    deploymentTarget: '14.0'
    configFiles:
      Nightly: Release.xcconfig
    bundleResourcePath: SMCHelper
    sources:
      - path: SMCHelper/Sources
    settings:
      base:
        PRODUCT_NAME: mactools-fan-smc-helper
        PRODUCT_BUNDLE_IDENTIFIER: "$(BUNDLE_IDENTIFIER_PREFIX).mactools.plugins.fan-control.smc-helper"
        GENERATE_INFOPLIST_FILE: true
        SWIFT_VERSION: 6.0
        MACOSX_DEPLOYMENT_TARGET: '14.0'
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: "$(DEVELOPMENT_TEAM)"
        OTHER_LDFLAGS: -framework IOKit
      configs:
        Nightly:
          PRODUCT_BUNDLE_IDENTIFIER: "$(BUNDLE_IDENTIFIER_PREFIX).mactools.plugins.fan-control.smc-helper.nightly"
          CREATE_INFOPLIST_SECTION_IN_BINARY: true
