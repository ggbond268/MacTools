---
release: app
type: added
area: Preferences
---

Fresh-setup preference restore now preselects missing plugins, preserves manual choices, shows determinate progress, and reports plugin settings that require a restart as deferred instead of failed.
