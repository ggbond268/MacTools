---
release: plugin
type: added
---

Added AI Assistant with configurable OpenAI-compatible providers (per-profile temperature and optional reasoning request) and editable prompt templates, a global shortcut per enabled prompt to process selected text, stable workflow/action-grid operations per enabled prompt, reasoning display for models that expose it, and Accessibility/Automation permissions guidance.
