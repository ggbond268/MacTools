---
release: plugin
type: fixed
---

The AI Assistant now asks before sending text that may have come from the clipboard, skips simulated-copy keystrokes when the target app lost focus, and no longer reopens a hidden panel on its own.

Custom OpenAI-compatible endpoints now require a complete IPv4 address before private-network checks apply.
