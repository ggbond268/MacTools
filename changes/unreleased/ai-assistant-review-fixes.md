---
release: plugin
type: fixed
---

The AI Assistant now asks before sending clipboard-derived text, preserves clipboard changes instead of restoring unverified content, and no longer reopens a hidden panel on its own.

Custom OpenAI-compatible endpoints now require a complete IPv4 address before private-network checks apply.
