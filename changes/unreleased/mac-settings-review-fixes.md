---
release: plugin
type: fixed
area: Mac Settings
---

Mac Settings preserves each trackpad's original preferences during Undo and failed-change recovery, skips unavailable history entries, and prevents partial profile replacement when restoring a backup fails.

Inline controls stay disabled during profile operations, rejected slider changes reset correctly, and profile editors show validation and save errors.

Provider-backed settings reflect current availability and offer navigation to the relevant plugin.
