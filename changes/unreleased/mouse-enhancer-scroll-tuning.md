---
release: plugin
type: added
area: Mouse Enhancer
---

Mouse Enhancer adds per-device scroll step and speed sliders, so tiny wheel ticks can be lifted to a minimum distance and scroll distance can be scaled up or down. Defaults keep the current behavior. Already-smoothed scroll events injected by remote-control software are left untouched so remote sessions keep their own feel.
